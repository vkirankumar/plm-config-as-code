apiVersion: v1
kind: Service
metadata:
  name: {{ include "plm-catalog-helm.fullname" . }}
  labels:
    {{- include "plm-catalog-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plm-catalog-helm.selectorLabels" . | nindent 4 }}
